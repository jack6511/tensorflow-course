title: tensorflow
description: learn tensorflow
template:
  repo: tensorflow-template
  name: tensorflow
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
